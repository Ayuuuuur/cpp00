#include "phonebook.hpp"

// bool phonebook::checkempty(cont &book){
//     if(book.first_name == "" || book.last_name == "" || book.nb == "" || book.nickname == "" || book.secret == "")
//         return false;
//     return true;
// }